#cover-list
    h5 标记列表
    table.table
        tr
            th 名称
            th 图形
            th 操作
        tr(v-for="cover in covers")
            td {{cover.name}}
            td {{cover.type}}
            td
                span.badge.badge-danger(v-on:click="deleteCover(cover)") 删除